import { type WebSocketTransportOptions } from './websocket-transport.js';
import type { SDKUserMessage } from '../types/messages.js';
import type { CanUseTool } from '../types/permissions.js';
import type { HookEvent, HookCallbackMatcher } from '../types/hooks.js';
import type { McpServerConfig } from '../types/mcp.js';
import type { Query } from '../types/options.js';
export type BrowserQueryOptions = {
    prompt: AsyncIterable<SDKUserMessage>;
    websocket: WebSocketTransportOptions & {
        url: string;
    };
    abortController?: AbortController;
    canUseTool?: CanUseTool;
    hooks?: Partial<Record<HookEvent, HookCallbackMatcher[]>>;
    mcpServers?: Record<string, McpServerConfig>;
    jsonSchema?: Record<string, unknown>;
};
/**
 * Create a Qoder query using WebSocket transport in the browser.
 *
 * Only SDK MCP servers (in-process) are supported in the browser.
 * Process-based MCP servers require Node.js and are not available.
 */
export declare function query(options: BrowserQueryOptions): Query;