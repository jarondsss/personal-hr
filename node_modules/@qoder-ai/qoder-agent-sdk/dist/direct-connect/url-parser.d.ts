export type DirectConnectUrlResult = {
    serverUrl: string;
    authToken?: string;
};
/**
 * Parse a direct-connect URL.
 *
 * Supported formats:
 * - cc://token@host:port/path -> { serverUrl: "http://host:port/path", authToken: "token" }
 * - cc://host:port/path -> { serverUrl: "http://host:port/path" }
 * - http://host:port/path -> { serverUrl: "http://host:port/path" }
 * - https://host:port/path -> { serverUrl: "https://host:port/path" }
 */
export declare function parseDirectConnectUrl(url: string): DirectConnectUrlResult;