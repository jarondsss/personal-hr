import type { Transport } from '../core/transport.js';
import type { StdoutMessage } from '../types/control.js';
export interface DirectConnectOptions {
    url: string;
    abortController?: AbortController;
    cwd?: string;
}
/**
 * DirectConnectTransport — Connects to a remote qodercli server via WebSocket.
 *
 * Flow:
 * 1. POST /sessions -> creates a session, gets { sessionId, workDir, wsUrl }
 * 2. WebSocket to wsUrl -> bidirectional JSONL communication
 */
export declare class DirectConnectTransport implements Transport {
    private options;
    private ws;
    private ready;
    private messages;
    private exitError?;
    private partialLine;
    private initializePromise;
    constructor(options: DirectConnectOptions);
    initialize(): Promise<void>;
    private doInitialize;
    write(data: string): Promise<void>;
    close(): void;
    isReady(): boolean;
    readMessages(): AsyncGenerator<StdoutMessage, void, unknown>;
    endInput(): void;
}