import type { PluginDetails } from '../types/plugins.js';
export declare function listPlugins(opts?: {
    orgId?: string;
    cliPath?: string;
    env?: Record<string, string>;
}): Promise<PluginDetails[]>;
export declare function enablePlugin(id: string, opts?: {
    cliPath?: string;
    env?: Record<string, string>;
}): Promise<void>;
export declare function disablePlugin(id: string, opts?: {
    cliPath?: string;
    env?: Record<string, string>;
}): Promise<void>;