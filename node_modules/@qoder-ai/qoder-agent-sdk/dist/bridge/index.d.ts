/**
 * Bridge module — remote session management.
 *
 * Provides functions for creating remote sessions and attaching to them
 * via the bridge API (SSE transport for reads, CCR client for writes).
 */
export { attachBridgeSession } from './bridge-session.js';
export { createCodeSession, fetchRemoteCredentials } from './bridge-api.js';
export type { SessionState, BridgeSessionHandle, AttachBridgeSessionOptions, RemoteCredentials, } from '../types/bridge.js';