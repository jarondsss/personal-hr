import type { AttachBridgeSessionOptions, BridgeSessionHandle } from '../types/bridge.js';
/**
 * Attach to an existing bridge session.
 * Creates the transport (SSE + CCR), wires routing, returns a session handle.
 */
export declare function attachBridgeSession(opts: AttachBridgeSessionOptions): Promise<BridgeSessionHandle>;