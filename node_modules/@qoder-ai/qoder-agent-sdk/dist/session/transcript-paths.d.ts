export declare const BRAND_CONFIG_DIR_INTERNAL = ".qoder";
export declare const ENV_HOME = "QODER_CLI_HOME";
export declare const ENV_CONFIG_DIR = "QODER_CONFIG_DIR";
export declare const MAX_SANITIZED_LENGTH = 200;
export declare function homedir(): string;
/**
 * Resolve the config directory that contains `projects/`.
 *
 * Resolution order:
 *   1. `QODER_CONFIG_DIR` — full path override (SDK-only escape hatch for
 *      tests / sandboxed setups; qodercli does not read this). When set,
 *      `BRAND_CONFIG_DIR_INTERNAL` is NOT appended — the value is used as-is.
 *   2. `<homedir()>/<BRAND_CONFIG_DIR_INTERNAL>` — default, aligned with
 *      qodercli's `Storage.getGlobalConfigDir()`.
 */
export declare function getConfigDir(): string;
export declare function getTranscriptProjectsDir(): string;
export declare function sanitizePath(name: string): string;