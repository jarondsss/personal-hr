---
name: Append to config files, don't overwrite
description: When editing Juan's config/skill files, append new content instead of replacing the whole file
type: feedback
---

When modifying Juan's config or skill files (e.g., SKILL.md), always append new content rather than overwriting the entire file.

**Why:** Juan rejected a Write that replaced SKILL.md because it deleted his existing personality config. He said "jangan diubah filenya tapi ditambahin" — don't change the file, add to it.

**How to apply:** Use Edit to append below existing content instead of Write to replace the whole file. Always preserve what's already there unless Juan explicitly says to remove something.
